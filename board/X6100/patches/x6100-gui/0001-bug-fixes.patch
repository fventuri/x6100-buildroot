--- a/src/clock.c
+++ b/src/clock.c
@@ -61,7 +61,7 @@
     }
 }
 
-static void show_time() {
+static void show_time(lv_timer_t * timer) {
     time_t      now;
     struct tm   *t;
 
@@ -151,7 +151,7 @@
     set_state(CLOCK_TIME);
     timeout = get_time() + params.clock_time_timeout * 1000;
 
-    show_time();
+    show_time(NULL);
     lv_timer_create(show_time, 500, NULL);
 
     last_time_sync = get_last_sync_time();
--- a/src/dialog_callsign.c
+++ b/src/dialog_callsign.c
@@ -65,12 +65,12 @@
         }
     }
     params_str_set(&params.callsign, callsign);
-    dialog_destruct(&dialog);
+    dialog_destruct();
     return true;
 }
 
 static bool edit_cancel() {
-    dialog_destruct(&dialog);
+    dialog_destruct();
     return true;
 }
 
@@ -101,12 +101,12 @@
 
     switch (key) {
         case LV_KEY_ESC:
-            dialog_destruct(&dialog);
+            dialog_destruct();
             break;
 
         case LV_KEY_ENTER:
             edit_ok();
-            dialog_destruct(&dialog);
+            dialog_destruct();
             break;
 
         case KEY_VOL_LEFT_EDIT:
--- a/src/dialog_freq.c
+++ b/src/dialog_freq.c
@@ -110,13 +110,13 @@
 
         case LV_KEY_ESC:
             voice_say_text_fmt("Frequency window has been closed");
-            dialog_destruct(&dialog);
+            dialog_destruct();
             break;
 
         case HKEY_FINP:
         case LV_KEY_ENTER:
             enter_freq();
-            dialog_destruct(&dialog);
+            dialog_destruct();
             break;
 
         case KEY_VOL_LEFT_EDIT:
--- a/src/dialog_ft8.c
+++ b/src/dialog_ft8.c
@@ -278,7 +278,7 @@
 
     switch (key) {
         case LV_KEY_ESC:
-            dialog_destruct(&dialog);
+            dialog_destruct();
             break;
 
         case KEY_VOL_LEFT_EDIT:
--- a/src/dialog_gps.c
+++ b/src/dialog_gps.c
@@ -345,7 +345,7 @@
 
     switch (key) {
         case LV_KEY_ESC:
-            dialog_destruct(&dialog);
+            dialog_destruct();
             break;
 
         case KEY_VOL_LEFT_EDIT:
--- a/src/dialog_msg_cw.c
+++ b/src/dialog_msg_cw.c
@@ -208,7 +208,7 @@
 
     switch (key) {
         case LV_KEY_ESC:
-            dialog_destruct(&dialog);
+            dialog_destruct();
             break;
 
         case KEYBOARD_F4:
--- a/src/dialog_msg_voice.c
+++ b/src/dialog_msg_voice.c
@@ -456,7 +456,7 @@
 
     switch (key) {
         case LV_KEY_ESC:
-            dialog_destruct(&dialog);
+            dialog_destruct();
             break;
 
         case KEY_VOL_LEFT_EDIT:
--- a/src/dialog_qth.c
+++ b/src/dialog_qth.c
@@ -37,12 +37,12 @@
         msg_update_text_fmt("Incorrect QTH Grid");
     }
 
-    dialog_destruct(&dialog);
+    dialog_destruct();
     return true;
 }
 
 static bool edit_cancel() {
-    dialog_destruct(&dialog);
+    dialog_destruct();
     return true;
 }
 
@@ -74,12 +74,12 @@
 
     switch (key) {
         case LV_KEY_ESC:
-            dialog_destruct(&dialog);
+            dialog_destruct();
             break;
 
         case LV_KEY_ENTER:
             edit_ok();
-            dialog_destruct(&dialog);
+            dialog_destruct();
             break;
 
         case KEY_VOL_LEFT_EDIT:
--- a/src/dialog_recorder.c
+++ b/src/dialog_recorder.c
@@ -53,7 +53,7 @@
 static void construct_cb(lv_obj_t *parent);
 static void destruct_cb();
 static void key_cb(lv_event_t * e);
-static void load_btn_page();
+static void load_btn_page(void *);
 
 static void update_level_cb(lv_timer_t * timer);
 
@@ -357,7 +357,7 @@
 
     switch (key) {
         case LV_KEY_ESC:
-            dialog_destruct(&dialog);
+            dialog_destruct();
             break;
 
         case KEY_VOL_LEFT_EDIT:
@@ -437,6 +437,6 @@
 }
 
 
-static void load_btn_page() {
+static void load_btn_page(void *) {
     buttons_load_page(&btn_page);
 }
--- a/src/dialog_swrscan.c
+++ b/src/dialog_swrscan.c
@@ -301,7 +301,7 @@
 
     switch (key) {
         case LV_KEY_ESC:
-            dialog_destruct(&dialog);
+            dialog_destruct();
             break;
 
         case KEY_VOL_LEFT_EDIT:
--- a/src/dialog_wifi.c
+++ b/src/dialog_wifi.c
@@ -229,7 +229,7 @@
     switch (key) {
 
     case LV_KEY_ESC:
-        dialog_destruct(&dialog);
+        dialog_destruct();
         break;
 
     case KEY_VOL_LEFT_EDIT:
--- a/src/knobs.cpp
+++ b/src/knobs.cpp
@@ -15,6 +15,7 @@
 #include <string>
 #include <vector>
 #include <map>
+#include <stdexcept>
 
 extern "C" {
     #include "styles.h"
--- a/src/scheduler.cpp
+++ b/src/scheduler.cpp
@@ -8,6 +8,7 @@
 
 #include "scheduler.h"
 
+#include <cstdlib>
 #include <queue>
 #include <mutex>
 
--- a/src/cfg/subjects.h
+++ b/src/cfg/subjects.h
@@ -18,6 +18,7 @@
 #include <mutex>
 #include <algorithm>
 #include <list>
+#include <string>
 #include <type_traits>
 #include <thread>
 #include <atomic>
--- a/src/meter.c
+++ b/src/meter.c
@@ -54,7 +54,7 @@
     *(bool*)user_data = subject_get_int(subj);
 }
 
-static void update_db_label() {
+static void update_db_label(void *) {
     lv_label_set_text_fmt(db_val_label, "%.1f", meter_db_raw);
 }
 
